import React from "react";
import styled from "styled-components";

const ChatInput = () => {
  return <ChatInputContainer></ChatInputContainer>;
};

export default ChatInput;

const ChatInputContainer = styled.div``;
